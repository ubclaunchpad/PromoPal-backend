import React, { ReactElement } from "react";

import Router from "./Router";

function App(): ReactElement {
  return (
    <div className="App">
      <Router />
    </div>
  );
}

export default App;
